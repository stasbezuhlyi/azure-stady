#Stages -> Jobs -> Steps -> Sasks
stages:
  - stage: build
    jobs:
      - job: getlatest
        steps:
          - script: echo "Hello!"
          - script: echo "Hello2!"
      - job: build
  - stage: test
    jobs:
      - job: test