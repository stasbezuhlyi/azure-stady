#Stages -> Jobs -> Steps -> Tasks
stages:
  - stage: build
    jobs:
      - job: getlatest
        steps:
          - script: echo "Hello!"
          - script: echo "Hello2!"
      - job: build
  - stage: test
    jobs:
      - job: test
        steps:
          - script: echo "Start test"
          - script: sleep 10
      - job: postest